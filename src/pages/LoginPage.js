import React from 'react';
import LoginForm from '../components/LoginForm'; // Asegúrate de que este import es correcto

const LoginPage = () => {
  return (
    <div className="container">
      <h2>Iniciar Sesión</h2>
      <LoginForm />
    </div>
  );
};

export default LoginPage;
